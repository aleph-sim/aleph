# Phase 5.9 — CUDA SV throughput → Aer-GPU parity

Phase 5 met its exit gate (aleph FP64 GPU SV within 1.22× of cuStateVec on every
Tier-1 + Tier-2 workload), but the P5-08 report found **Qiskit Aer-GPU is 2–5×
faster than both aleph and cuStateVec** — same FP64, its own engine (not
cuStateVec), the advantage entirely **above the kernel**: gate fusion + batched
dispatch (`docs/perf/p5-08-gpu-report.md`). Phase 5.9 closes that gap. This page
tracks it; the exit metric is in `ROADMAP.md` §7.

> **Result (P5.9-05).** With all levers enabled, **5 of 7 Tier-1+Tier-2 cells are
> now within 1.5× of Aer-GPU** (was 2–5× behind on every cell), and aleph **beats
> cuStateVec by 1.7–4.4×** on the dense/Grover workloads. The exit metric is
> **partially met**: only QFT (1.73×) and QPE (1.75×) remain over, both blocked on
> the one unbuilt lever — a GPU diagonal phase-polynomial kernel for the
> controlled-phase ladder. Full table and verdict in [§ P5.9-05](#p59-05--verdict-vs-aer-gpu).

## P5.9-01 — feed the existing IR fusion passes into the GPU path

`fuse_for_gpu()` runs the GPU-applicable fusion sub-pipeline — `CancelInversePairs`
→ `DeadCodeElim` → `Fuse1qRuns` → `Fuse2q` — before the backend. These emit
`Unitary1q` / `Unitary2q` blocks, which the CUDA backend already applies (they
carry a `GateMatrix`, unlike `UnitaryKq`). **No new kernels.** Fewer gates ⇒ fewer
full-state passes, the dominant cost at scale.

Correctness: `tests/gpu_fusion_bench.rs::gpu_fused_matches_cpu_unfused` pins the
fused GPU run against the unfused CPU `NaiveSvBackend` at 1e-10.

### Result (RTX 4000 SFF Ada, FP64, n=28, best of 4)

| workload     | gates (before→after) | unfused (s) | fused (s) | speedup | gap to Aer-GPU |
|--------------|----------------------|------------:|----------:|--------:|---------------:|
| QFT          | 406 → 406            | 10.54       | 10.54     | 1.00×   | 1.73× (unchanged) |
| random (d20) | 1100 → 540           | 49.88       | 30.37     | **1.64×** | 2.15× → **1.30×** |
| VQE (8L)     | 664 → 242            | 29.53       | 14.42     | **2.05×** | 2.89× → **1.42×** |
| QAOA (p4)    | 476 → 138            | 22.39       | 9.02      | **2.48×** | 3.42× → **1.38×** |

The free step (no new kernels) closes most of the dense-workload gap: from 2.1–3.4×
behind Aer-GPU to **1.3–1.4×**. QFT is untouched — its controlled-phase ladder is
diagonal and doesn't collapse under `Fuse1q`/`Fuse2q`; it needs the diagonal path
below.

## P5.9-02a — apply dense `UnitaryKq` (k≤5) on the GPU

`FuseKq` (P2-07) collapses adjacent gates into dense `2^k × 2^k` `UnitaryKq`
blocks, but both CUDA SV backends *rejected* them: `UnitaryKq` has no fixed-size
`GateMatrix` (the enum stops at 8×8 / k=3), so `gate.matrix()` returns
`Unrepresentable`. The kernels themselves already handled k≤5 (`apply_kq`'s
`qbit[5]`/`sorted[5]` + a `k>5` guard; `custatevecApplyMatrix` takes arbitrary
`nTargets`).

P5.9-02a wires the two together: both backends intercept `Gate::UnitaryKq`
before validation and route the raw row-major slice through a shared
`validate_kq` + `flatten_kq` (operands MSB-first, identical to the `Unitary2q`
path). Oracle: `tests/gpu_unitary_kq.rs` runs `FuseKq{max_qubits:5}` on
dense_chain/random/vqe, applies on the GPU, and matches the unfused CPU
`NaiveSvBackend` at 1e-10 — asserting the fused circuits actually contain k=4
and k=5 blocks so the new path is exercised. **Infrastructure only; the
throughput choice is P5.9-02b.**

## P5.9-02b — feed `FuseKq` into the GPU path; width-3 is the sweet spot

`fuse_for_gpu` now appends `FuseKq{max_qubits: MAX_FUSE_QUBITS}` after
`Fuse1q`/`Fuse2q`. `FuseKq` emits a dense block only at span ≥ 3 with ≥ 2
members, so it is purely additive over P5.9-01 (isolated 1q/2q gates keep their
specialised kernels). The A/B knob `fuse_for_gpu_with(c, kq_max)` sweeps the
width (`None` = the P5.9-01 baseline).

### A/B result (RTX 4000 SFF Ada, FP64, n=28, best of 5)

Speedups are vs the P5.9-01 baseline (Fuse1q+Fuse2q, no ≥3q blocks).

| workload     | baseline gates / s | k=3 gates / s / spd | k=4 spd | k=5 spd |
|--------------|-------------------:|--------------------:|--------:|--------:|
| QFT          | 406 / 10.51        | 406 / 10.53 / 1.00× | 1.00×   | 1.00×   |
| random (d20) | 540 / 30.21        | 277 / 26.52 / **1.14×** | 1.01× | 0.78×   |
| VQE (8L)     | 242 / 14.35        | 137 / 13.05 / **1.10×** | 0.94× | 0.68×   |
| QAOA (p4)    | 138 / 8.98         |  81 / 8.32 / **1.08×**  | 0.93× | 0.71×   |

**k=3 wins 1.08–1.14× on every dense workload; k=4 and k=5 regress** (down to
0.68×). The generic `apply_kq` does a dense `2^k × 2^k` matvec per amplitude
group — O(4^k) work, 4× more at k=5 than k=3 — and its `v[32]`/`gidx[32]`
thread-local arrays (768 B at k=5) spill to local memory. Past k=3 that extra
compute overtakes the fewer-passes savings, even though gate counts keep
dropping (random: 540 → 277 → 282 → 186). So `MAX_FUSE_QUBITS = 3`. QFT is
diagonal and unaffected (no ≥3q dense runs to fuse), as in P5.9-01.

Aer reaches its k=5 default with **register-tiled fused-block kernels**; matching
that needs a kernel that keeps the `2^k` block in registers/shared memory
without spilling — a P5.9-04-class lever. Until then k=3 is the GPU sweet spot,
and the correct (oracle-pinned) k=4,5 apply path from P5.9-02a stays available
for when that kernel lands.

## P5.9-03 — batched disjoint-1q-layer dispatch

A circuit layer of `m` single-qubit gates on *distinct* qubits is `m` separate
full-state passes under per-gate dispatch — `m` `apply_1q` launches, each
streaming all `2^n` amplitudes. `apply_1q_multi` collapses a width-`b` chunk into
**one** sweep: gather the `2^b` local block, apply each gate as a butterfly along
its own local bit (`O(b·2^b)` work — no dense matvec), scatter back. A layer of
`m` disjoint 1q gates becomes `⌈m/b⌉` sweeps instead of `m`. `CudaSvBackend::run_layered`
greedily batches maximal runs of plain disjoint 1q gates (any other instruction,
or a second gate on a pending qubit, flushes first so program order is exact).

Oracle: `tests/gpu_layer_oracle.rs` pins `run_layered` against the per-gate CPU
`NaiveSvBackend` at 1e-10 on the dense workloads plus an adversarial circuit
(same-qubit run, 1q/2q interleave, diagonal-in-batch, barrier, controlled-1q),
and verifies batch widths 1..5 are bit-for-bit invariant.

### A/B result (RTX 4000 SFF Ada, FP64, n=28, best of 5)

Speedup vs per-gate `run`. `pure1q` is a 2q-free stack of 1q layers (max effect).

| workload      | per-gate (s) | b=2  | b=3      | b=4  | b=5  |
|---------------|-------------:|-----:|---------:|-----:|-----:|
| pure1q (d40)  | 42.26        | 1.50× | **1.52×** | 1.52× | 1.05× |
| random (d20)  | 49.97        | 1.18× | **1.19×** | 1.18× | 1.03× |
| VQE (8L)      | 29.41        | 1.26× | **1.26×** | 1.25× | 1.03× |
| QAOA (p4)     | 22.38        | 1.08× | **1.08×** | 1.08× | 1.00× |

`b=2/3/4` tie at the top; **`b=5` collapses to ~1.0×** — the strided `2^5=32`
gather + register spill erase the fewer-passes win, the same wall the P5.9-02b
k-sweep hit at k=5. So `DEFAULT_LAYER_BATCH = 3`. The gain is **additive over
fusion** (it cuts launches *within* a 1q sublayer, which fusion does not touch):
1.52× on pure-1q stacks, 1.08–1.26× on the realistic dense mix.

## P5.9-04 — specialised CNOT permutation kernel

A CNOT is a permutation, not a rotation: it swaps the two amplitudes that differ
in the target bit wherever the control bit is 1. The generic `apply_kq` path
treats it as a dense 4×4 matvec — gather 4 amplitudes per group, 16 complex
mults, scatter 4 — over **all** `2^n` amplitudes. The new `apply_cnot` kernel
instead swaps the target pair directly in the **control=1 half** of the state:
zero FLOPs, and it touches only `2^(n-1)` amplitudes (the control=0 half is
identity, never read). A plain CNOT (no external controls) routes here; anything
controlled/Toffoli still uses `apply_kq`.

Oracle: `tests/gpu_cnot_oracle.rs` pins it against the per-gate CPU
`NaiveSvBackend` (1e-10) **and** the backend's own dense path
(`with_custom_2q(false)`, 1e-12), across both operand orderings (control < target
and control > target) and a |10>→|11> basis-state truth table.

### A/B result (RTX 4000 SFF Ada, FP64, n=28, best of 5)

`apply_cnot` vs the dense `apply_kq` 4×4 path on the same circuit.

| workload        | dense 4×4 (s) | apply_cnot (s) | speedup |
|-----------------|--------------:|---------------:|--------:|
| cnot_only (d40) | 49.42         | 22.94          | **2.15×** |
| random (d20)    | 50.04         | 32.12          | **1.56×** |
| QAOA (p4)       | 22.35         | 15.61          | **1.43×** |

The ~2× on CNOT-only matches the model exactly (half the memory traffic, no
matvec); the dense workloads gain in proportion to their CNOT share (random is
~½ CNOT, QAOA's entangling layers are CNOT–Rz–CNOT). Stacks with the layer
batcher and fusion — each cuts a different cost.

## P5.9-05 — verdict vs Aer-GPU

Re-ran the P5-08 harness with **all Phase-5.9 levers** enabled (fusion + layer
batching + the CNOT kernel) against cuStateVec and Aer-GPU on the full Tier-1 +
Tier-2 suite. The "aleph 5.9" column is the better of two configurations per
workload (both timed in `tests/gpu_report59_bench.rs`):

- **layered-raw** = `run_layered(circuit)` — wins where the CNOT permutation
  kernel matters (random).
- **layered-fused** = `run_layered(fuse_for_gpu(circuit))` — wins where fewer
  passes dominate (grover, VQE, QAOA).

### Tier-1 + Tier-2 (RTX 4000 SFF Ada, FP64, n = 28, best of 5)

| workload      | aleph 5.9 (s) | cuStateVec (s) | Aer-GPU (s) | aleph / Aer | exit ≤1.5× | tier |
|---------------|--------------:|---------------:|------------:|------------:|:----------:|:----:|
| GHZ           | 3.46          | 3.50           | 2.92        | 1.18×       | PASS       | T1   |
| QFT           | 10.54         | 10.56          | 6.09        | **1.73×**   | OVER       | T1   |
| Grover (4 it) | 4.32          | 19.15          | 3.94        | 1.10×       | PASS       | T1   |
| random (d=20) | 24.49         | 41.16          | 23.74       | 1.03×       | PASS       | T1   |
| QPE           | 11.01         | 11.52          | 6.28        | **1.75×**   | OVER       | T2   |
| VQE (8L)      | 12.75         | 25.92          | 10.19       | 1.25×       | PASS       | T2   |
| QAOA (p=4)    | 7.98          | 19.28          | 6.53        | 1.22×       | PASS       | T2   |

**Exit metric (ROADMAP §7): aleph FP64 GPU SV within 1.5× of Aer-GPU on every
cell, no Tier-1 cell worse than 2×.**

**Verdict: PARTIALLY MET.** 5 of 7 cells are now within 1.5× of Aer-GPU — up from
the P5-08 baseline where Aer-GPU was **2–5× faster than aleph on every cell**. The
dense-2q half (random, VQE, QAOA) and Grover are at parity (1.03–1.25×), and there
aleph now **beats cuStateVec by 1.7–4.4×**. The "no Tier-1 cell worse than 2×"
guard holds (worst Tier-1 = QFT 1.73×). But the headline ≤1.5×-on-**every**-cell
clause fails on **QFT (1.73×) and QPE (1.75×)** — and only those.

Both misses are the same shape: a long **controlled-phase ladder** that is
diagonal but does *not* collapse under `Fuse1q`/`Fuse2q`/`FuseKq` (no dense ≤3q
block forms), so the pass count stays high while Aer's engine streams it cheaply.
This is the one Phase-5.9 lever left unbuilt — **diagonal-run fusion + a GPU
phase-polynomial kernel** (`FuseDiagonalRuns` → one `amps[x] *= exp(i·φ(x))`
sweep; remaining lever #1 below). It is the precondition for closing QFT/QPE, and
the only thing between the current result and a full exit-metric pass.

## Remaining levers (where the rest of the gap is)

1. **Diagonal-run fusion (QFT/QPE).** `FuseDiagonalRuns` collapses a phase ladder
   into one `DiagonalPhase` instruction (a phase polynomial over ≤64 qubits). A
   GPU kernel that evaluates `amps[x] *= exp(i·φ(x))` in one sweep — building on
   the P5-06 diagonal kernels — turns QFT's ~350 cphase passes into ~1. This is
   the single biggest lever for the diagonal half (QFT is 1.73× behind on nothing
   but pass count). → **P5.9-02**.
2. **Register-tiled fused-block kernel (unlock k=4,5).** P5.9-02a/02b shipped the
   `UnitaryKq` GPU apply path and fed `FuseKq` into the pipeline, but measured
   that the *generic* `apply_kq` only profits up to **k=3** (1.08–1.14×); k=4,5
   regress because the dense per-group matvec + register spill outgrows the
   pass-count savings. A kernel that holds the `2^k` block in registers/shared
   memory (Aer-style) is the precondition for raising `MAX_FUSE_QUBITS` toward
   Aer's 5. → **P5.9-04**-class.
3. ~~**Disjoint-layer single-sweep dispatch.**~~ **Done (P5.9-03):** `run_layered`
   batches disjoint 1q sublayers via `apply_1q_multi` — 1.52× on pure-1q stacks,
   1.08–1.26× on the dense mix, additive over fusion. Width 3 (b=5 regresses).
4. ~~**Specialised 2q kernels** (CNOT permutation, diagonal-2q).~~ **Done
   (P5.9-04):** `apply_cnot` is 2.15× over the dense path on CNOT-only, 1.43–1.56×
   on the dense mix; diagonal-2q (CZ/CRz/CPhase) was already covered by the P5-06
   diagonal kernels.

Reproduce:

```bash
# P5.9-01 (unfused vs Fuse1q+Fuse2q):
ALEPH_REPORT_N=28 cargo test -p aleph-cuda --features cuda --release \
  -- --ignored --nocapture gpu_fusion_bench

# P5.9-02b (P5.9-01 baseline vs FuseKq k=3/4/5):
ALEPH_REPORT_N=28 cargo test -p aleph-cuda --features cuda --release \
  -- --ignored --nocapture gpu_fuse_kq_bench

# P5.9-03 (per-gate baseline vs run_layered b=2/3/4/5):
ALEPH_REPORT_N=28 cargo test -p aleph-cuda --features cuda --release \
  -- --ignored --nocapture gpu_layer_bench

# P5.9-04 (dense 4×4 baseline vs apply_cnot):
ALEPH_REPORT_N=28 cargo test -p aleph-cuda --features cuda --release \
  -- --ignored --nocapture gpu_cnot_bench

# P5.9-05 verdict (aleph all-levers vs cuStateVec; + Aer column from python):
ALEPH_REPORT_N=28 cargo test -p aleph-cuda --features cuquantum --release \
  -- --ignored --nocapture gpu_report59
ALEPH_REPORT_N=28 /root/aervenv/bin/python crates/aleph-cuda/tests/gpu_report_bench.py
```
