# Phase 4 — Surface-code syndrome extraction (stabilizer)

> Auto-generated by `scripts/surface_code/render_report.py`. Do not edit by hand.

**Date:** 2026-06-12  
**Host:** AMD EPYC 8124P 16-Core Processor (single-thread, RAYON_NUM_THREADS=1)  
**Toolchain:** rustc 1.95.0 (598076161 2026-04-14), RUSTFLAGS=-C target-cpu=native; Stim 1.16.0 TableauSimulator (median of 50 runs)

_Single-thread both sides. aleph = `StabilizerBackend` (CHP tableau, O(n²)); baseline = Stim `TableauSimulator`. One rotated surface-code syndrome-extraction cycle (2d²−1 qubits). **Stim is a purpose-built, heavily-optimised stabilizer simulator; aleph is expected to be slower per cycle** — the deliverable is correctness parity (postselected stabilizer-group equivalence, `surface_code_stim_oracle`) plus a documented time-per-cycle row, not beating Stim._

## Time per cycle

| d | qubits | aleph (ms) | Stim (ms) | aleph / Stim |
|--:|-------:|-----------:|----------:|-------------:|
| 3 | 17 | 0.002 | 0.008 | 0.22× |
| 5 | 49 | 0.006 | 0.013 | 0.48× |
| 7 | 97 | 0.020 | 0.023 | 0.86× |
| 9 | 161 | 0.046 | 0.057 | 0.81× |
| 11 | 241 | 0.088 | 0.111 | 0.79× |

<!-- P3-08 / P3-11 / P4.5-02 addenda (word-parallel rowsum, gates, then
     row-helpers + AVX-512 transpose). Notes appended by hand; the table above
     is regenerated by render_report.py and now reflects the P4.5-02 numbers. -->

### P3-08: word-parallel + SIMD `rowsum`

P3-08 replaced the scalar per-bit `rowsum` (the CHP measurement hot path) with a
word-parallel `u64` kernel plus an AVX-512 + VPOPCNTQ kernel (runtime-dispatched).
Measured before/after on the same idle EPYC box, single-thread:

| d | aleph before (ms) | aleph after (ms) | cycle speedup | aleph/Stim before → after |
|--:|------------------:|-----------------:|--------------:|--------------------------:|
| 3 | 0.006 | 0.004 | 1.46× | 0.77× → 0.53× |
| 5 | 0.052 | 0.033 | 1.56× | 3.94× → 2.55× |
| 7 | 0.212 | 0.134 | 1.59× | 9.38× → 5.92× |
| 9 | 0.603 | 0.368 | 1.64× | 10.64× → 6.56× |
| 11 | 1.375 | 0.842 | **1.63×** | **12.52× → 7.66×** |

**Hard target (P3-08 design): surface-d11 ≤ 2× Stim — NOT met by P3-08 (7.66×).**
The rowsum work landed its share — a real 1.63× cycle speedup at d=11 — but did not
close the gap to ≤2×, because `rowsum` was never the majority of the cycle. A
`perf record` of the d=11 cycle *after* P3-08 attributed time as:

```
70.9%  Tableau::cnot      (gate kernel — per-row scalar column bit-twiddle)
15.3%  Tableau::h         (gate kernel)
 5.3%  Tableau::measure   (contains rowsum)
 3.5%  Tableau::zero_row
 2.6%  Tableau::copy_row
```

The cycle was **gate-bound (cnot+h ≈ 86%)**; the measurement/`rowsum` path was
~5–11%. Closing the remaining gap requires word-parallelizing the **gate** kernels
(CNOT/H), which touch a single column across all rows (strided/gather under the
row-major layout) — explicitly out of P3-08's scope. That is P3-11.

### P3-11: word-parallel + SIMD gate kernels (dual-orientation tableau)

P3-11 picks up the gate half. The tableau is now **lazily dual-orientation**
(ADR 0013): gates run in a column-major layout where a qubit column is a
contiguous `W = ceil((2n+1)/64)`-word span, so H/S/CNOT/Paulis update the whole
column **word-parallel** (scalar `u64` + AVX-512 `avx512f`, runtime-dispatched);
`rowsum`/`measure` keep the row-major P3-08 path; a blocked 64×64 bit-transpose
bridges the two on orientation change (~2 transposes/cycle for a batched cycle).
`sign` is orientation-invariant (packed bit-vector), updated word-parallel inside
the gate kernels. Before/after on the same idle EPYC box, single-thread (the
"before" column is a fresh same-session re-measurement of pre-P3-11 `main`, so its
aleph/Stim ratios differ by ≲1% from the P3-08 addendum's "after" column above —
run-to-run variance, not a regression):

| d | aleph before (ms) | aleph after (ms) | cycle speedup | aleph/Stim before → after |
|--:|------------------:|-----------------:|--------------:|--------------------------:|
| 3 | 0.004 | 0.003 | 1.33× | 0.53× → **0.40×** |
| 5 | 0.033 | 0.011 | 3.17× | 2.55× → **0.81×** |
| 7 | 0.133 | 0.037 | 3.57× | 5.84× → **1.63×** |
| 9 | 0.370 | 0.092 | 4.01× | 6.60× → **1.65×** |
| 11 | 0.848 | 0.181 | **4.69×** | 7.71× → **1.64×** |

**Hard target ≤ 2× Stim at d=11 — MET (1.64×).** P3-11 takes surface-d11 from
P3-08's 7.66× to **1.64× Stim** (a 4.69× cycle speedup); d=7/9/11 are all within
2×, and at d=3/5 aleph is now *faster* than Stim. A fresh `perf record` of the
d=11 cycle confirms the gate kernels are no longer the bottleneck:

```
29.7%  BitGrid::transpose   (the new orientation-bridge cost)
26.0%  Tableau::measure
19.1%  Tableau::zero_row     (still scalar per-bit column clear)
13.5%  Tableau::copy_row     (still scalar per-bit row copy)
 5.5%  Tableau::rowsum
 1.6%  Tableau::cnot         (was 70.9% under P3-08)
 0.4%  Tableau::h            (was 15.3% under P3-08)
```

Gates dropped from ~86% to ~2%. The remaining time is the transpose bridge (~30%)
plus the measurement-collapse helpers `zero_row`/`copy_row` (still row-major
per-bit, ~33% combined) — the natural next levers, but ≤2× is already met so they
are deferred. Correctness is unchanged: ColMajor scalar + AVX-512 gate kernels are
bit-exact vs the preserved row-major `*_scalar` references (proptest; AVX-512 path
exercised on EPYC), the blocked transpose is bit-exact vs a scalar reference
(incl. the 483×241 d=11 shape), and all Stim oracles pass at d=3..11.

### P4.5-02: word-parallel `zero_row`/`copy_row` + AVX-512 transpose

P4.5-02 takes the two levers P3-11 deferred. (1) `Tableau::zero_row` and
`Tableau::copy_row` were still per-bit loops over `n` columns even though a
RowMajor generator row is `stride = ceil(n/64)` *contiguous* words — they
become a word `fill(0)` and a `copy_from_slice` via `row_pair_mut` (the same
split-borrow used by `rowsum`). (2) The 64×64 block kernel inside
`BitGrid::transpose` gains an AVX-512 variant: the whole block is 64 u64 =
8 zmm registers, so Warren's delta-swap network runs entirely in-register
(levels j=32/16/8 pair whole vectors lane-for-lane; j=4/2/1 pair lanes via
`vpermq` + mask-blends), runtime-dispatched like `rowsum_dispatch` with the
scalar kernel kept for non-AVX-512 hosts. Before/after on the same idle EPYC
box, single-thread, same-session Stim 1.16.0 (median of 50 runs):

| d | aleph before (µs) | aleph after (µs) | cycle speedup | aleph/Stim before → after |
|--:|------------------:|-----------------:|--------------:|--------------------------:|
| 3 | 3.20 | 1.83 | 1.75× | 0.39× → **0.22×** |
| 5 | 10.52 | 6.36 | 1.66× | 0.80× → **0.48×** |
| 7 | 37.45 | 19.77 | 1.89× | 1.63× → **0.86×** |
| 9 | 92.72 | 46.37 | 2.00× | 1.62× → **0.81×** |
| 11 | 180.96 | 88.21 | **2.05×** | 1.63× → **0.79×** |

**Phase-4.5 target ≤ 1.2× Stim — MET with room to spare: aleph is now
*faster* than Stim at every distance, 0.79× at d=11** (a 2.05× cycle
speedup over P3-11). The d=7..11 "before" ratios (1.62–1.63×) re-measured
this session match the P3-11 addendum (1.63–1.65×) — same-box consistency.
A fresh `perf record` of the d=11 cycle (self time, ≥1%):

```
57.8%  Tableau::measure     (per-bit strided column scans + collapse loop)
16.7%  BitGrid::transpose   (orientation bridge, now AVX-512)
11.3%  Tableau::rowsum
 3.7%  BitGrid::row_pair_mut
 3.4%  Tableau::cnot
 2.0%  dispatch::apply_gate
```

`zero_row`/`copy_row` (19.1% + 13.5% under P3-11) are gone from the profile
(< 1%). The dominant remaining cost is `Tableau::measure`'s own per-bit
column scans (`x.get(row, a)` over all rows to find/collect anticommuting
stabilizers — strided single-bit reads under RowMajor), a possible future
lever, but with every cell ahead of Stim there is no parity motivation to
chase it. Correctness: the word-parallel row helpers are bit-exact vs the
preserved `#[cfg(test)]` `*_scalar` references (full-word compares incl.
padding bits, irregular n ∈ {3, 70, 130, 241}, mutation-tested); the AVX-512
transpose kernel is bit-exact vs `transpose64` (64 cases incl. corner bits,
exercised on EPYC); all Stim oracles green at d=3..11 on EPYC
(`surface_code_stim_oracle`, `stim_oracle`, `stim_measure_oracle`).

### P4.6-02: batched Pauli-frame sampler

Multi-shot `Backend::sample` previously re-ran the full CHP measurement once per
shot. P4.6-02 batches it: every shot of one `sample` call shares ONE x/z tableau
— whether a qubit measures random or deterministic depends only on the
stabilizer *structure* (x/z), never on the measured *signs* — so the O(n²) x/z
rowsum work runs once per 64-shot batch and per-shot variation lives entirely in
64-bit **sign words**. The sign-word update is the scalar one with the structural
flip bit `(phase mod 4 == 2)` broadcast across all 64 lanes; this is the per-shot
algorithm with `sign → word`, so the distribution is provably unchanged.

`Tableau::sample_frames(qubits, shots, rng)` returns column-major frame words
(64 shots/word, no qubit-count limit); `sample_qubits_batched` is the ≤64-qubit
per-shot transpose used by `Backend::sample`.

**AC-1 — surface-d11 cycle, 1024 shots, 120 ancillas (local M-series, aarch64
scalar):**

| path | time |
| --- | --- |
| sequential (per-shot clone + measure) | 65.70 ms |
| batched (`sample_frames`) | 1.07 ms |

**≈61×** — far past the ≥10× target. The win is the 64×-per-batch amortization
of the shared x/z rowsum work, structural and platform-independent (EPYC's
AVX-512 `rowsum_dispatch` accelerates both paths equally). *[EPYC confirmation
pending an idle box.]*

**Correctness (AC-2/AC-3):** a stabilizer state's Z-basis distribution is
*exactly uniform* over its support, so `crates/aleph-stab/tests/frame_sampler.rs`
checks the batched sampler against that exact reference (5σ band) over GHZ/Bell,
a mixed 5-qubit Clifford, a subset measurement, a partial final batch, and a
16-case random-Clifford proptest; same seed → identical frame table. The frame
path is thus proven equal to the per-shot/tableau path, which the Stim oracles
(`surface_code_stim_oracle`, `stim_measure_oracle`) already validate against Stim
— so frame ≡ Stim transitively. Non-Clifford circuits never reach `sample`: the
backend rejects them at `apply_gate`. Pauli-noise (P4.6-04) will hook an
X/sign-frame injection at `measure_word`; the seam is left visible, not built.
