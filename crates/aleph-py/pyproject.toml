[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "aleph"
requires-python = ">=3.12"
version = "0.0.0"

[tool.maturin]
features = ["python"]
module-name = "aleph"
